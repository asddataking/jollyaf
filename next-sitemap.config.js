// next-sitemap.config.js
// Uncomment and configure when ready to add sitemap generation

/*
const nextSitemapConfig = {
  siteUrl: 'https://bookjollyaf.com',
  generateRobotsTxt: true,
  robotsTxtOptions: {
    policies: [
      {
        userAgent: '*',
        allow: '/',
      },
    ],
  },
}

module.exports = nextSitemapConfig
*/

// To use:
// 1. Install: npm install next-sitemap
// 2. Add to package.json scripts: "postbuild": "next-sitemap"
// 3. Uncomment this file
